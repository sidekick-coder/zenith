import Module from '#client/entities/module.entity.ts'
import './assets/css/styles.css'

const files = import.meta.glob<any>('./hooks/*.hook.ts', { eager: true })

const subhooks = Object.values(files)
    .map((mod) => mod.default)
    .map(Hook => new Hook())

export default class {{ upperCaseName }} extends Module {
    public subhooks = subhooks
}
