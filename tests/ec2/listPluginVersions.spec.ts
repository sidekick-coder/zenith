import { test, expect, Page } from '@playwright/test'
// import { GenericContainer, StartedTestContainer } from 'testcontainers'


test.fixme('should list a plugin versions', async ({ page }) => {

})

